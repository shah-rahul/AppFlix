import 'package:flutter/material.dart';

class MovieDetailScreeen extends StatelessWidget {
  static const routeName = "/movie-detail";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("this movie"),
      ),
    );
  }
}
